/** App version string — used in branding and document title. */
export const EPIPHANY_VERSION = '0.2'

/** Display title for browser / PWA. */
export const APP_TITLE = `Epiphany ${EPIPHANY_VERSION} — Learn Greek`
